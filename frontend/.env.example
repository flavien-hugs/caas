# Backend base URL. Server-side fetches go straight to this host; the
# browser only sees the SvelteKit Node server.
BACKEND_BASE=http://localhost:8080

# Public version of the same URL — used only by `pnpm gen:api`.
PUBLIC_API_BASE=http://localhost:8080
